
@{
    ViewData["Title"] = "NoDatabase";
}

<body>

    <script>
        function noJournal() {
            Swal.fire({
                icon: "error",
                title: "No database found!",
                text: "It seems that journal.sqlite is missing",
                allowOutsideClick: false,
            }).then((result) => {
                if (result.isConfirmed) {
                    window.parent.close();
                }
            });
        }
    </script>
    @Html.Raw(TempData["msg"])

</body>